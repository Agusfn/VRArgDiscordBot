
/**
 * For future use.
 */
class SlashCommands {

    test() {
        /*await (await Discord.getGuild().commands.fetch("921154065424064532")).delete()
        Discord.getGuild().commands.create({
            name: "Eliminar ScoreSaber",
            //description: "un comando de prueba. que se yo",
            type: ApplicationCommandTypes.USER
        })
        let commands = await Discord.getGuild().commands.fetch()
        console.log("commands: ", commands)*/
    }

}